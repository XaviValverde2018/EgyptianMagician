using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DamageShopButton : MonoBehaviour {  
    
    //this code is in damage shop Button
    /*
     1. Clicar al botó augmentem +5 al playerpref: "DamagePP" 
     2. Tancar el set active
     3. restar els diners. Comprovar si tenim diners per a poder comprar.  
    */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
